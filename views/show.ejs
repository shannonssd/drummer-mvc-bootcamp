<!DOCTYPE html>
<html lang="en">
  <head>
    <style>
      body {
        height: 100vh;
      }
      .name {
        font-size: 40px;
        font-weight: bold;
        display: flex;
        flex-direction: column;
        height: 100%;
        justify-content: center;
        align-items: center;
      }
    </style>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Drummer Profile</title>
  </head>
  <body>
    <div class="name">
      <%= user.name %>
      <div>
        <form action="/reservation/<%= user.id %>" method="POST">
          <label for="date"></label>
          <input type="date" id="date" name="date" />
          <input type="submit" value="Click here to make a resevation" />
        </form>
      </div>
      <div>
        View list of <%= user.name %>'s
        <a href="/reservations/<%= user.id %>">BOOKINGS</a>
      </div>
      <button><a href="/">Home</a></button>
    </div>
  </body>
</html>
